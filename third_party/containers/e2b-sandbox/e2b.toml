# This is a config for E2B sandbox template.
# You can use 'template_id' (785n69crgahmz0lkdw9h) or 'template_name (wsai_code) from this config to spawn a sandbox:

# Python SDK
# from e2b import Sandbox
# sandbox = Sandbox(template='wsai_code')

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create({ template: 'wsai_code' })

dockerfile = "Dockerfile"
template_name = "wsai_code"
template_id = "785n69crgahmz0lkdw9h"
